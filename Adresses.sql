CREATE TABLE [dbo].[Adresses] (
    [AddressId]     INT          NOT NULL,
    [Location]      VARCHAR (25) NOT NULL,
    [CityYouBelong] VARCHAR (25) NOT NULL,
    PRIMARY KEY CLUSTERED ([AddressId] ASC)
);


GO

